name: Health Checks

on:
  workflow_dispatch:
  schedule:
    - cron: '0 9 * * *'

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: bloglist_health_check
        uses: jtalk/url-health-check-action@v5
        with:
          url: https://bloglist-r47q.onrender.com/
